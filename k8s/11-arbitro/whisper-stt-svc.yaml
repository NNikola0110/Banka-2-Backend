apiVersion: v1
kind: Service
metadata:
  name: whisper-stt-svc
  namespace: banka2-tim2
  labels:
    app.kubernetes.io/name: whisper-stt
    app.kubernetes.io/component: arbitro
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: whisper-stt
  ports:
    - port: 8093
      targetPort: 8093
      name: http
