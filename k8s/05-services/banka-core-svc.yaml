apiVersion: v1
kind: Service
metadata:
  name: banka-core-svc
  namespace: banka2-tim2
  labels:
    app.kubernetes.io/name: banka-core
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: banka-core
  ports:
    - port: 8080
      targetPort: 8080
      name: http
