apiVersion: v1
kind: Service
metadata:
  name: influxdb-svc
  namespace: banka2-tim2
  labels:
    app.kubernetes.io/name: influxdb
    app.kubernetes.io/component: timeseries-db
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: influxdb
  ports:
    - port: 8086
      targetPort: 8086
      name: http
